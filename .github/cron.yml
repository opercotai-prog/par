name: Update Posts Cron

on:
  schedule:
    - cron: '*/30 * * * *' # Запуск каждые 30 минут
  workflow_dispatch: # Позволяет запустить вручную через кнопку во вкладке Actions

jobs:
  run-parser:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install telethon supabase httpx

      - name: Run parser task
        env:
          # Ваши старые секреты
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_SESSION_STRING: ${{ secrets.TG_SESSION_STRING }}
          # НОВЫЕ секреты с буквой E, чтобы не было конфликтов
          SUPABASEE_URL: ${{ secrets.SUPABASEE_URL }}
          SUPABASEE_KEY: ${{ secrets.SUPABASEE_KEY }}
        run: python task_runner.py
